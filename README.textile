p. Created by Andrea Campolonghi on 2010-08-18
Copyright (c) 2010 Railo Technologies. All rights reserved

h4. TODO:

* verify hits

h4. BUILD:

* Add jetty to the project root.
* Add a file called unames.xml in /webroot/extensions/:

pre. <?xml version="1.0" encoding="iso-8859-1"?>
<config>
	<server>
		<password>andrea</password>
	</server>
</config>

p. Place the password used in the railo server used for test.


h4. TEST:

p. After installing the extension create 2 cache from MongoDbDriver:

* mongotest default for objects
* mongotest_template default for templates
 
h4. Mongodb replica set config
 
pre. mongod --replSet foo --port 27017 --dbpath /data/r0
 mongod --replSet foo --port 27018 --dbpath /data/r1
 mongod --replSet foo --port 27019 --dbpath /data/r2
 > config = {_id: 'foo', members: [
                          {_id: 0, host: 'localhost:27017'},
                          {_id: 1, host: 'localhost:27018'},
                          {_id: 2, host: 'localhost:27019'},
                          {_id: 4, host: 'localhost:27020', votes : 4, arbiterOnly: true}
                          ]
           }
> rs.initiate(config);